<UserControl x:Class="EasyVideoEdition.View.ViewChoiceOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasyVideoEdition.View"
             mc:Ignorable="d" 
 d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="Open" Margin="10px" Command="{Binding GoToOpenCommand}"/>
        <Button Grid.Column="1" Content="View 2" Margin="10px"  IsEnabled="{Binding editVideoEnabled}"/>
        <Button Grid.Column="2" Content="View 3" Margin="10px" />
        <Button Grid.Column="3" Content="View 4" Margin="10px" />
        <Button Grid.Column="4" Content="Save" Margin="10px" Command="{Binding GoToSaveCommand}" />
    </Grid>
</UserControl>
